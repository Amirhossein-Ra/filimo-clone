import { Link } from "react-router-dom";

const Login = () => {
  return (
    <Link to="/Login" className="text-white bg-green-600 rounded-md p-2 px-5">
      Loggin
    </Link>
  );
};

export default Login;
